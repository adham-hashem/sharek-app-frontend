import React, { useState } from 'react';
import { ActivityIndicator, StyleSheet, Text, TextInput, TouchableOpacity, View } from 'react-native';
import { router } from 'expo-router';
import { ScreenHeader } from '@/components/ScreenHeader';
import { useAuth } from '@/lib/auth';
import { colors, radius, spacing, typography } from '@/lib/theme';
export default function ForgotPasswordScreen() { const { resetPassword, language } = useAuth(); const [identifier, setIdentifier] = useState(''); const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false); const ar = language === 'ar'; const submit = async () => { if (!identifier.trim()) { setMessage(ar ? 'اكتب البريد الإلكتروني أو رقم الهاتف.' : 'Enter your email or phone number.'); return; } setBusy(true); const result = await resetPassword(identifier); setBusy(false); setMessage(result.error ? (ar ? 'تعذر إرسال الرابط. حاول مرة أخرى.' : 'Could not send the link. Please try again.') : (ar ? 'تم إرسال رابط آمن إلى البريد المرتبط بالحساب. افحص البريد والرسائل غير المرغوب فيها.' : 'A secure link was sent to the email linked to your account. Check your inbox and spam folder.')); }; return <View style={styles.container}><ScreenHeader title={ar ? 'نسيت كلمة المرور' : 'Forgot password'} onBack={() => router.back()} /><View style={styles.card}><Text style={styles.title}>{ar ? 'استعادة كلمة المرور' : 'Recover your password'}</Text><Text style={styles.help}>{ar ? 'أدخل البريد الإلكتروني أو رقم الهاتف، وسنرسل رابطًا آمنًا إلى بريدك.' : 'Enter your email or phone. We will send a secure link to your email.'}</Text><TextInput style={styles.input} value={identifier} onChangeText={setIdentifier} autoCapitalize="none" autoCorrect={false} keyboardType="email-address" placeholder={ar ? 'البريد الإلكتروني أو رقم الهاتف' : 'Email or phone number'} placeholderTextColor={colors.brownMuted} />{message ? <Text style={styles.message}>{message}</Text> : null}<TouchableOpacity style={styles.button} onPress={submit} disabled={busy}>{busy ? <ActivityIndicator color={colors.white} /> : <Text style={styles.buttonText}>{ar ? 'إرسال رابط الاستعادة' : 'Send recovery link'}</Text>}</TouchableOpacity></View></View>; }
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: colors.background }, card: { margin: spacing.lg, padding: spacing.lg, backgroundColor: colors.surface, borderRadius: radius.lg, gap: spacing.md }, title: { ...typography.heading, color: colors.brown, textAlign: 'center' }, help: { ...typography.body, color: colors.brownMuted, textAlign: 'center' }, input: { ...typography.body, color: colors.brown, borderWidth: 1, borderColor: colors.border, borderRadius: radius.md, padding: spacing.md }, message: { ...typography.small, color: colors.primary, textAlign: 'center' }, button: { backgroundColor: colors.primary, borderRadius: radius.md, padding: spacing.md, alignItems: 'center' }, buttonText: { ...typography.bodyBold, color: colors.white } });
